{{include 'page/functions.html'}}

<h1>
    Posts
</h1>
{{=LOAD('page', 'post_form.load', ajax=True, args=[user_id,])}}
<div id="posts">
    {{if 'posts' in globals():}}
        {{for row in posts:}}
            {{get_post(row, user_dict)}}
        {{pass}}
    {{pass}}
</div>

