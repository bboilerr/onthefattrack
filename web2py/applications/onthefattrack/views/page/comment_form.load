{{if 'form' in globals():}}

    {{from applications.onthefattrack.modules import util}}

    {{if util.contains_all(request.vars, ('text',)):}}
        <script>
            $(document).ready(function () {
//                updatePosts({{=form.vars.id}});

 //               hidePostForm();
            });
        </script>
    {{pass}}

    <div class="comment_form" style="display:none;">
        {{=form}}
    </div>

{{pass}}

