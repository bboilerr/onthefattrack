{{if 'form' in globals():}}

    {{if 'text' in request.vars:}}
        <div class="comment_{{=form.vars.id}}"></div>

        <script>
            $(document).ready(function () {
                comments_div = $('.comment_{{=form.vars.id}}').closest('.post').find('.comments');

                updateComments(comments_div, {{=form.vars.id}});
            });
        </script>

    {{pass}}

    <div class="comment_form" style="display:none;">
        {{=form}}
    </div>

{{pass}}

