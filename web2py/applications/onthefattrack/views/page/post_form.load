{{if 'form' in globals():}}

{{if 'name' in request.vars and 'text' in request.vars and not form.errors:}}
<script>
    $(document).ready(function () {
        var name = '{{=XML(request.vars.name)}}';
        var text = '{{=MARKMIN(request.vars.text)}}';
        var date = new Date();

        updatePosts(name, date, text);
    });
</script>
{{pass}}

{{=form}}
{{pass}}
